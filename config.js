require('dotenv').config()

module.exports = {
    prefix: "cov",
	token: process.env.TOKEN,
    owner: process.env.OWNER,
    //All prefix of messages goes here
    prefix_searchSlotwithPinCode : 'slots in pincode'
};